#pragma once
#include <"gameData.h">
#include <fstream>
class dynamicArray{
private:
    chart* chartArray = nullptr;
    ifstream fin;
    ofstream fout;
public:
    
    fout.open("chart.txt");
 int size = 0;
cout << Date:   Time:   Players: << endl;
    if (input.is_open()) {
        string line;
        while (getline(input, line)) {
            cout << line << endl;
            ptr[size] = line;
            size++;
        }
        input.close();
  fout.close();

};